/**
 * React high order components for the Grid system
 * @namespace
 * @name Hoc
 * @memberOf module:Grid.Components
 */

import { convertToNodesHolder } from './NodesHolder';

export {
    convertToNodesHolder
};
